-- ============================================================
-- TorgStore Analytics — Migration 006
-- Удаляет неиспользуемые таблицы: customers, sales, audit_log.
--
-- Причина: построены на старте проекта (001_initial_schema.sql) под
-- когортный анализ/LTV и историю изменений, но ни разу не были подключены
-- ни к одному импорту данных (нет ни одной ручки, которая бы наполняла
-- customers или писала в audit_log; sales наполнялась только вручную по
-- одной записи через POST /sales, которым фронтенд ни разу не пользовался).
-- Подтверждено аудитом бэкенда (26.07.2026) и явным решением пользователя
-- удалить, а не оставлять как задел на будущее.
--
-- Идемпотентная миграция — безопасно запускать повторно.
-- ============================================================

DROP TABLE IF EXISTS sales CASCADE;
DROP TABLE IF EXISTS customers CASCADE;
DROP TABLE IF EXISTS audit_log CASCADE;
