-- ============================================================
-- TorgStore Analytics — Migration 009
-- Фото менеджера в едином досье («FBI-карточка» — см. product_rhythm_model.md
-- принцип «история по имени»/case file). Только поле под URL/data-URL —
-- саму загрузку делает пользователь вручную через UI (PATCH /managers/{id}/photo),
-- это не CRM-импорт и не автоматизация.
-- Идемпотентная миграция — безопасно запускать повторно.
-- ============================================================

ALTER TABLE managers ADD COLUMN IF NOT EXISTS photo_url TEXT;
